# Local Development Configuration for Web App

# API Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:3006
NEXT_PUBLIC_API_TIMEOUT=10000

# Web app port (should be different from API port)
PORT=3001

# Development settings
NODE_ENV=development

# Optional: Supabase configuration for client-side features
# NEXT_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=<your-supabase-anon-key>

# Notes:
# - This file is a template for local development
# - Copy to .env.local and adjust values as needed
# - NEXT_PUBLIC_ variables are exposed to the browser
# - Never put sensitive secrets in NEXT_PUBLIC_ variables