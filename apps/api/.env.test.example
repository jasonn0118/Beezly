# Test Environment Configuration Template
# Copy this file to .env.test and update with your test values

NODE_ENV=test
PORT=3001

# Test Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=root
DB_PASSWORD=root
DB_NAME=beezly_test
DB_SSL=false
DB_LOGGING=false

# Azure Form Recognizer Configuration (if needed for testing)
AZURE_FORM_RECOGNIZER_ENDPOINT=https://your-resource.cognitiveservices.azure.com
AZURE_FORM_RECOGNIZER_API_KEY=your-azure-api-key

# OpenAI Configuration (optional for testing)
# Can use test/staging API key or omit for mock testing
OPENAI_API_KEY=test-openai-api-key

# Google Places API Configuration (optional for testing)
# Can use test/staging API key or omit for mock testing
GOOGLE_PLACES_API_KEY=test-google-places-api-key

# JWT Configuration (test-only secret)
JWT_SECRET=test_jwt_secret_for_testing_only

# Supabase Configuration (dummy values for testing)
SUPABASE_URL=https://dummy-project.supabase.co
SUPABASE_ANON_KEY=dummy-anon-key-for-testing
SUPABASE_SERVICE_ROLE_KEY=dummy-service-role-key-for-testing
