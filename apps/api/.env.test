# Local Development Environment Configuration
NODE_ENV=development
PORT=3001

# Local PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=root
DB_PASSWORD=root
DB_NAME=beezly_test
DB_SSL=false
DB_LOGGING=true

# Azure Form Recognizer Configuration (if needed for local testing)
AZURE_FORM_RECOGNIZER_ENDPOINT=https://your-resource.cognitiveservices.azure.com
AZURE_FORM_RECOGNIZER_API_KEY=your-azure-api-key

# JWT Configuration (for local testing)
JWT_SECRET=local_jwt_secret_for_development_only

# Supabase Configuration (if needed for storage/auth in local)
SUPABASE_URL=your_local_supabase_url
SUPABASE_ANON_KEY=your_local_supabase_anon_key